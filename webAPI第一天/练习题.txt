1 判断用户输入的邮箱地址是否含有“@”符号

2.判断一下字符串中每一个字符是不是图片（.jpg就可以）

3. var arr=["杞"，"枸"，"泡"，"里"，"杯"，"温"，"保"，"，"，"已"，"得"，"不"，"年"，"中"，"到"，"人"];
	将此数组结果改为字符串的形式倒序输出。

4 求圆的面积（用函数做）

5  下面的代码会输出什么结果。
	function test() {
    console.log(a);
    console.log(foo());
    var a = 1;
    function foo() {
       return 2;
    }
 }
test();

6：动态改变一个图片的显示；